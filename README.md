# FULL-STACK-USING-PYTHON-REACT-MONGODB
A full-stack web application built with Python (Flask/Django) on the backend, React.js on the frontend, and MongoDB as the database — following the PRM (Python-React-MongoDB) stack architecture.
